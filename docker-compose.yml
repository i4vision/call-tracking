version: '3.8'

services:
  app:
    build: .
    container_name: call_tracking_app
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # Map the Windows host directory to the internal container directory
      - C:\OpenPhoneArchive:/archive:ro
    env_file:
      - backend/.env
